// this is a comment
log_level = "debug"

local.file "token" {
  is_secret = true
  clustering = true
  replicas = 5

  nested_block {
    testing = "this is a string"
  }
}

unlabeled.block {
  this_is_unlabeled = ["string1", local.file.reciever, false]
}
